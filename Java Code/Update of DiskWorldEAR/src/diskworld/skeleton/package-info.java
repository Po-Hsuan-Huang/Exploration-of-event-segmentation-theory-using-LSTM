/**
 * The skeleton of a DiskComplex encodes the neighboring information (which pieces are connected to which). Here temporary merges of DiskComplexes (grasping) are computed. 
 */
package diskworld.skeleton;